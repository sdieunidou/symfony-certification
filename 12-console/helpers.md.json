{
  "source_file": "12-console/helpers.md",
  "title": "Helpers Natifs",
  "questions": [
    {
      "id": 1,
      "question": "Quelle classe de 'Haut Niveau' est recommandée pour gérer l'affichage et l'interaction dans la console, remplaçant l'utilisation directe des Helpers individuels ?",
      "type": "single_choice",
      "options": [
        "ConsoleHelper",
        "SymfonyStyle ($io)",
        "OutputFormatter",
        "CommandStyle"
      ],
      "correct_answers": [
        "SymfonyStyle ($io)"
      ],
      "explanation": "`SymfonyStyle` est la surcouche standard qui offre un design cohérent et simplifie l'accès aux tableaux, barres de progression, et questions."
    },
    {
      "id": 2,
      "question": "Quel Helper permet d'afficher des données structurées sous forme de grille ASCII ?",
      "type": "single_choice",
      "options": [
        "GridHelper",
        "Table",
        "ListHelper",
        "MatrixHelper"
      ],
      "correct_answers": [
        "Table"
      ],
      "explanation": "Le helper `Table` permet de construire des tableaux avec en-têtes et lignes formatées."
    },
    {
      "id": 3,
      "question": "Quelle est la classe utilisée pour ajouter une ligne de séparation horizontale à l'intérieur d'un tableau ?",
      "type": "single_choice",
      "options": [
        "TableLine",
        "TableSeparator",
        "HLine",
        "RowSeparator"
      ],
      "correct_answers": [
        "TableSeparator"
      ],
      "explanation": "On insère une instance de `new TableSeparator()` dans le tableau des lignes (`setRows`) pour créer une séparation."
    },
    {
      "id": 4,
      "question": "Comment accède-t-on historiquement à un Helper (ex: 'question') depuis l'objet `$this` dans une commande ?",
      "type": "single_choice",
      "options": [
        "`$this->getHelper('question')`",
        "`$this->question()`",
        "`$this->helpers['question']`",
        "On doit l'instancier avec `new QuestionHelper()`."
      ],
      "correct_answers": [
        "`$this->getHelper('question')`"
      ],
      "explanation": "Les commandes possèdent un `HelperSet` accessible via la méthode `getHelper()`."
    },
    {
      "id": 5,
      "question": "Lors de l'utilisation du `ProgressBar`, quelles méthodes faut-il appeler pour mettre à jour et clore la barre ?",
      "type": "single_choice",
      "options": [
        "`update()` et `end()`",
        "`advance()` (dans la boucle) et `finish()` (à la fin)",
        "`next()` et `stop()`",
        "`step()` et `close()`"
      ],
      "correct_answers": [
        "`advance()` (dans la boucle) et `finish()` (à la fin)"
      ],
      "explanation": "`advance()` incrémente le compteur et redessine la barre. `finish()` assure que la barre atteint 100% et ajoute un saut de ligne final."
    },
    {
      "id": 6,
      "question": "Quel helper permet d'exécuter un processus système et d'afficher sa sortie en temps réel ?",
      "type": "single_choice",
      "options": [
        "ProcessHelper",
        "ExecHelper",
        "ShellHelper",
        "CommandHelper"
      ],
      "correct_answers": [
        "ProcessHelper"
      ],
      "explanation": "ProcessHelper wrappe le composant Process pour l'usage CLI."
    },
    {
      "id": 7,
      "question": "Comment ajouter une ligne de séparation dans un tableau rendu par `Table` ?",
      "type": "single_choice",
      "options": [
        "En insérant `new TableSeparator()` dans les rows",
        "En appelant `addSeparator()`",
        "En passant `['---']`",
        "Impossible"
      ],
      "correct_answers": [
        "En insérant `new TableSeparator()` dans les rows"
      ],
      "explanation": "TableSeparator crée une ligne horizontale."
    },
    {
      "id": 8,
      "question": "Quel helper gère l'interactivité (questions, autocomplétion, mot de passe masqué) ?",
      "type": "single_choice",
      "options": [
        "QuestionHelper",
        "PromptHelper",
        "AskHelper",
        "InputHelper"
      ],
      "correct_answers": [
        "QuestionHelper"
      ],
      "explanation": "QuestionHelper est utilisé par SymfonyStyle pour les entrées."
    },
    {
      "id": 9,
      "question": "Pourquoi préférer `$io->table()` plutôt que `new Table($output)` ?",
      "type": "single_choice",
      "options": [
        "Moins de code et look cohérent avec les autres sorties SymfonyStyle",
        "Table est déprécié",
        "Table ne gère pas les headers",
        "Obligatoire en prod"
      ],
      "correct_answers": [
        "Moins de code et look cohérent avec les autres sorties SymfonyStyle"
      ],
      "explanation": "SymfonyStyle encapsule les helpers et harmonise l'affichage."
    },
    {
      "id": 10,
      "question": "Comment récupérer un helper historique depuis une commande ?",
      "type": "single_choice",
      "options": [
        "`$this->getHelper('question')` via le HelperSet",
        "`$io->helper('question')`",
        "`HelperRegistry::get()`",
        "Impossible"
      ],
      "correct_answers": [
        "`$this->getHelper('question')` via le HelperSet"
      ],
      "explanation": "getHelper() accède au HelperSet de la commande."
    }
  ]
}